import { EditorialDetail } from '@/components/editorial-pages'; import { getEditorial } from '@/lib/editorial'; import { publicMetadata } from '@/lib/seo'; export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const entry=await getEditorial('changelog',slug);return publicMetadata(entry?.meta.title??'Change',entry?.meta.description??'Change unavailable.',`/changelog/${slug}`,!entry)} export default async function Page({params}:{params:Promise<{slug:string}>}){return <EditorialDetail collection="changelog" slug={(await params).slug}/>}
