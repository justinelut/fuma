import { ContactForm } from '@/components/contact-form';import { PageMain } from '@/components/site-shell';import { publicMetadata } from '@/lib/seo';export const metadata=publicMetadata('Privacy request','Send a bounded privacy request to Fuma.','/privacy-request',true);export default function Page(){return <PageMain className="max-w-4xl"><h1 className="text-5xl font-semibold">Privacy request</h1><p className="mt-5 text-lg text-muted-foreground">Use this route to ask about access, correction or deletion. We may need to verify your request through a separate safe process.</p><ContactForm kind="privacy"/></PageMain>}
