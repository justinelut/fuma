import { Breadcrumbs } from '@/components/breadcrumbs';import { CTA, Tag } from '@/components/public-sections';import { PageMain } from '@/components/site-shell';import { readPublicData } from '@/lib/public-data';import { publicMetadata } from '@/lib/seo';import { notFound } from 'next/navigation';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const data=await readPublicData('showcases',{limit:100});const item=data?.data.items.find(v=>v.slug===slug);return publicMetadata(item?.title??'Showcase',item?.summary??'Showcase unavailable.',`/showcase/${slug}`,!item)}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const data=await readPublicData('showcases',{limit:100});const item=data?.data.items.find(v=>v.slug===slug);if(!item)notFound();return <PageMain className="max-w-5xl"><Breadcrumbs items={[{label:'Showcase',href:'/showcase'},{label:item.title,href:`/showcase/${item.slug}`}]} /><p className="font-mono text-xs uppercase tracking-widest text-muted-foreground">Approved case study</p><h1 className="mt-4 text-5xl font-semibold">{item.title}</h1><p className="mt-5 max-w-3xl text-lg text-muted-foreground">{item.summary}</p><div className="mt-5 flex flex-wrap gap-2">{[...item.profiles,...item.industries].map(tag=><Tag key={tag}>{tag}</Tag>)}</div><div className="mt-8"><CTA href={item.previewUrl}>Visit public work</CTA></div></PageMain>}
