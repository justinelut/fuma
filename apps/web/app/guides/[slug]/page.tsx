import { EditorialDetail } from '@/components/editorial-pages'; import { getEditorial } from '@/lib/editorial'; import { publicMetadata } from '@/lib/seo'; export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const entry=await getEditorial('guides',slug);return publicMetadata(entry?.meta.title??'Guide',entry?.meta.description??'Guide unavailable.',`/guides/${slug}`,!entry)} export default async function Page({params}:{params:Promise<{slug:string}>}){return <EditorialDetail collection="guides" slug={(await params).slug}/>}
