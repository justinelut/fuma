import { describe,expect,test } from 'bun:test';import { PublicPricingCatalogEnvelopeSchema } from '@fuma/public-contracts';import { Value } from '@sinclair/typebox/value';import { activePromotion,formatKes,isImmutableTemplatePreview,visiblePricing } from '../lib/public-data';import { expiredPricingEnvelope,pricingEnvelope } from './fixtures/public-web';describe('authoritative public commerce presentation',()=>{test('accepts only strict display-safe pricing envelopes',()=>{expect(Value.Check(PublicPricingCatalogEnvelopeSchema,pricingEnvelope)).toBe(true);expect(Value.Check(PublicPricingCatalogEnvelopeSchema,{...pricingEnvelope,privateOffer:true})).toBe(false);expect(Value.Check(PublicPricingCatalogEnvelopeSchema,{...pricingEnvelope,data:{...pricingEnvelope.data,items:[{...pricingEnvelope.data.items[0],providerPlanId:'forbidden'}]}})).toBe(false)});test('suppresses expired plans rather than retaining stale amounts',()=>{expect(visiblePricing(pricingEnvelope,new Date('2026-07-26T08:00:00Z'))).toHaveLength(1);expect(visiblePricing(expiredPricingEnvelope,new Date('2026-07-26T08:00:00Z'))).toHaveLength(0);expect(visiblePricing(null)).toEqual([])});test('requires isolated immutable template previews',()=>{expect(isImmutableTemplatePreview('https://sample.preview.fuma.co.ke/releases/release_1/')).toBe(true);expect(isImmutableTemplatePreview('https://fuma.co.ke/templates/live')).toBe(false);expect(isImmutableTemplatePreview('https://sample.preview.fuma.co.ke/draft')).toBe(false)});test('formats projected minor units using KES math',()=>{expect(formatKes(250000)).toMatch(/2,500/)});test('suppresses inactive promotions',()=>{const plan={...pricingEnvelope.data.items[0],promotion:{label:'Window',startsAt:'2026-07-01T00:00:00Z',endsAt:'2026-07-20T00:00:00Z'}};expect(activePromotion(plan,new Date('2026-07-26T00:00:00Z'))).toBeNull()})})
