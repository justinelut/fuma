import { describe,expect,test } from 'bun:test';import { PublicAcquisitionEventSchema,PublicHandoffRequestSchema } from '@fuma/public-contracts';import { Value } from '@sinclair/typebox/value';import { ContactRequestSchema,ConsentPreferenceSchema,PublicWebVitalSchema } from '../lib/public-web-contracts';describe('public transition and privacy contracts',()=>{test('handoff is a closed union without arbitrary redirects or identity',()=>{const safe={kind:'create_site',source:'home',profile:'website'};expect(Value.Check(PublicHandoffRequestSchema,safe)).toBe(true);expect(Value.Check(PublicHandoffRequestSchema,{...safe,redirect:'https://attacker.test'})).toBe(false);expect(Value.Check(PublicHandoffRequestSchema,{...safe,email:'person@example.test'})).toBe(false);expect(Value.Check(PublicHandoffRequestSchema,{kind:'sign_in',source:'direct',audience:'admin'})).toBe(false)});test('contact requires bounded consent and replay token',()=>{const safe={kind:'general',name:'A User',email:'user@example.test',message:'A bounded public request.',consentVersion:'2026-07-26',replayToken:'abcdefghijklmnop'};expect(Value.Check(ContactRequestSchema,safe)).toBe(true);expect(Value.Check(ContactRequestSchema,{...safe,message:'x'})).toBe(false);expect(Value.Check(ContactRequestSchema,{...safe,recipient:'private@example.test'})).toBe(false)});test('analytics rejects identifiers, payment data and unknown events',()=>{const safe={version:1,kind:'page_view',routeClass:'home',timestamp:'2026-07-26T08:00:00Z',consent:'not_required'};expect(Value.Check(PublicAcquisitionEventSchema,safe)).toBe(true);for(const extra of [{email:'a@b.test'},{tenantId:'tenant'},{amount:10},{staffId:'staff'},{kind:'fingerprint'}])expect(Value.Check(PublicAcquisitionEventSchema,{...safe,...extra})).toBe(false)});test('consent and vital payloads remain minimized',()=>{expect(Value.Check(ConsentPreferenceSchema,{version:1,choice:'essential',updatedAt:'2026-07-26T08:00:00Z'})).toBe(true);expect(Value.Check(PublicWebVitalSchema,{name:'LCP',value:900,rating:'good',routeClass:'home'})).toBe(true);expect(Value.Check(PublicWebVitalSchema,{name:'LCP',value:900,rating:'good',routeClass:'home',sessionId:'cross-host'})).toBe(false)})})
