import{useMemo}from'react';import type{FumaScopedShellReadyContext}from'../FumaScopedShell';import{OrganizationManagementClient}from'./client';import{OrganizationManagement}from'./OrganizationManagement'
export function OrganizationManagementRouteContent({shell}:{shell:FumaScopedShellReadyContext}){const organizationId=shell.resolution.selection.organizationId;const client=useMemo(()=>new OrganizationManagementClient(organizationId),[organizationId]);if(shell.routeAccess.kind!=='allowed'||shell.routeAccess.route.id!=='route.organization-management')return null;return <div data-testid="organization-management-route"><OrganizationManagement client={client}/></div>}
