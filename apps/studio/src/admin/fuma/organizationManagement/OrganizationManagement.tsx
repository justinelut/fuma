import{useCallback,useEffect,useState,type FormEvent}from'react';import{Button}from'@ui/components/Button';import{getErrorMessage}from'@core/utils/errorMessage';import{OrganizationManagementClient,type OrganizationManagementProjection}from'./client';import styles from'./OrganizationManagement.module.css'
export function OrganizationManagement({client}:{client:OrganizationManagementClient}){const[data,setData]=useState<OrganizationManagementProjection|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false),[inviteEmail,setInviteEmail]=useState(''),[inviteRole,setInviteRole]=useState<'admin'|'member'>('member'),[workspaceName,setWorkspaceName]=useState(''),[workspaceSlug,setWorkspaceSlug]=useState('');const load=useCallback(async()=>{setLoading(true);setError('');try{setData(await client.load())}catch(e){setError(getErrorMessage(e,'Organization management could not be loaded.'))}finally{setLoading(false)}},[client]);useEffect(()=>{queueMicrotask(()=>{void load()})},[load]);const run=async(work:()=>Promise<unknown>,message:string)=>{setBusy(true);setError('');setNotice('');try{await work();setNotice(message);await load()}catch(e){setError(getErrorMessage(e,'Organization change failed.'))}finally{setBusy(false)}};if(loading&&!data)return<p className={styles.status} role="status">Loading organization and workspaces…</p>;if(!data)return<p className={styles.error} role="alert">{error}</p>;const canManage=data.actorRole==='owner';async function invite(event:FormEvent){event.preventDefault();await run(()=>client.invite(inviteEmail,inviteRole),'Invitation created through Better Auth.');setInviteEmail('')}async function createWorkspace(event:FormEvent){event.preventDefault();await run(()=>client.workspace({kind:'create',name:workspaceName,slug:workspaceSlug}),'Workspace created.');setWorkspaceName('');setWorkspaceSlug('')}
return <section className={styles.root} aria-labelledby="organization-management-title"><header><p>Better Auth organization authority</p><h1 id="organization-management-title">Organization & team</h1><span>{data.organization.name} · {data.actorRole}</span></header>{notice?<p className={styles.notice} role="status">{notice}</p>:null}{error?<p className={styles.error} role="alert">{error}</p>:null}<div className={styles.grid}><section className={styles.panel}><h2>Organization</h2><form onSubmit={e=>{e.preventDefault();const form=new FormData(e.currentTarget);void run(()=>client.updateOrganization(String(form.get('name')),String(form.get('slug'))),'Organization updated through Better Auth.')}}><label>Name<input name="name" defaultValue={data.organization.name} disabled={!canManage||busy}/></label><label>Slug<input name="slug" defaultValue={data.organization.slug} disabled={!canManage||busy}/></label><Button type="submit" variant="secondary" disabled={!canManage||busy}>Save organization</Button></form></section><section className={styles.panel}><h2>Invite teammate</h2><form onSubmit={e=>void invite(e)}><label>Email<input type="email" required value={inviteEmail} disabled={!canManage||busy} onChange={e=>setInviteEmail(e.target.value)}/></label><label>Role<select value={inviteRole} disabled={!canManage||busy} onChange={e=>setInviteRole(e.target.value as typeof inviteRole)}><option value="member">Member</option><option value="admin">Admin</option></select></label><Button type="submit" variant="secondary" disabled={!canManage||busy||!inviteEmail}>Send invitation</Button></form><small>Customer invitations can grant admin or member only. Platform owner is never available here.</small></section></div><section className={styles.panel}><h2>Members</h2><div className={styles.table}><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Actions</th></tr></thead><tbody>{data.members.map(member=><tr key={member.id}><td>{member.name}</td><td>{member.email}</td><td>{member.role}</td><td><div className={styles.actions}>{member.role!=='owner'?<><select aria-label={`Role for ${member.name}`} disabled={!canManage||busy} value={member.role} onChange={e=>void run(()=>client.updateRole(member.id,e.target.value as'admin'|'member'),'Member role updated through Better Auth.')}><option value="member">Member</option><option value="admin">Admin</option></select><Button variant="ghost" size="sm" disabled={!canManage||busy} onClick={()=>void run(()=>client.remove(member.id),'Member removed through Better Auth.')}>Remove</Button></>:<span>Protected owner role</span>}</div></td></tr>)}</tbody></table></div></section><section className={styles.panel}><h2>Pending invitations</h2>{data.invitations.length?<ul>{data.invitations.map(item=><li key={item.id}><span><strong>{item.email}</strong><small>{item.role??'member'} · expires {new Date(item.expiresAt).toLocaleDateString()}</small></span><Button variant="ghost" size="sm" disabled={!canManage||busy} onClick={()=>void run(()=>client.cancelInvitation(item.id),'Invitation cancelled through Better Auth.')}>Cancel</Button></li>)}</ul>:<p>No pending invitations.</p>}</section><section className={styles.panel}><h2>Workspaces</h2><form className={styles.inline} onSubmit={e=>void createWorkspace(e)}><label>Name<input required value={workspaceName} disabled={!canManage||busy} onChange={e=>setWorkspaceName(e.target.value)}/></label><label>Slug<input required value={workspaceSlug} pattern="[a-z0-9]+(?:-[a-z0-9]+)*" disabled={!canManage||busy} onChange={e=>setWorkspaceSlug(e.target.value)}/></label><Button type="submit" variant="secondary" disabled={!canManage||busy||!workspaceName||!workspaceSlug}>Create workspace</Button></form><ul>{data.workspaces.map(workspace=><li key={workspace.id}><span><strong>{workspace.name}</strong><small>/{workspace.slug} · {workspace.status}{workspace.isDefault?' · default':''}</small></span><div className={styles.actions}>{!workspace.isDefault&&workspace.status==='active'?<Button variant="ghost" size="sm" disabled={!canManage||busy} onClick={()=>void run(()=>client.workspace({kind:'set-default',workspaceId:workspace.id}),'Default workspace changed.')}>Make default</Button>:null}{workspace.status==='active'?<Button variant="ghost" size="sm" disabled={!canManage||busy||workspace.isDefault} onClick={()=>void run(()=>client.workspace({kind:'archive',workspaceId:workspace.id}),'Workspace archived.')}>Archive</Button>:<Button variant="ghost" size="sm" disabled={!canManage||busy} onClick={()=>void run(()=>client.workspace({kind:'restore',workspaceId:workspace.id}),'Workspace restored.')}>Restore</Button>}</div></li>)}</ul></section>{!canManage?<p className={styles.status}>Read-only organization membership. Owner authority is required for changes.</p>:null}</section>}
