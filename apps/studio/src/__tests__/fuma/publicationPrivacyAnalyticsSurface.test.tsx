import { afterEach,expect,test } from 'bun:test'
import { cleanup,render,screen,waitFor } from '@testing-library/react'
import { PublicationAnalyticsSurface } from '../../admin/fuma/publication/PublicationAnalyticsSurface'

const report={range:{from:'2040-02-01',to:'2040-02-02'},retention:{rawEventDays:30 as const,aggregateDays:400 as const},totals:{siteReads:1,postReads:2,publicReads:2,memberReads:1,newsletterOpens:3,newsletterClicks:1,subscriptions:1,unsubscriptions:0},content:[{contentId:'post-1',publicReads:1,memberReads:1,totalReads:2}],referrers:[{referrer:'direct' as const,reads:1},{referrer:'internal' as const,reads:0},{referrer:'newsletter' as const,reads:1},{referrer:'search' as const,reads:1},{referrer:'social' as const,reads:0},{referrer:'other' as const,reads:0}],memberSources:[{source:'registered' as const,reads:0},{source:'complimentary' as const,reads:1},{source:'manual' as const,reads:0},{source:'paid' as const,reads:0}],newsletters:[{newsletterId:'daily',opens:3,clicks:1,subscriptions:1,unsubscriptions:0}]}
afterEach(()=>cleanup())
test('FUMA-040 Studio dashboard exposes labelled range, aggregate tables, policy, and deterministic export',async()=>{const client={report:async()=>report,export:async()=>({fileName:'publication-analytics-2040-02-01-2040-02-02.csv',mediaType:'text/csv; charset=utf-8' as const,sha256:'a'.repeat(64),bytes:2,content:'x\n'})};render(<PublicationAnalyticsSurface client={client}/>);expect(screen.getByRole('heading',{name:'Privacy-aware analytics'})).toBeDefined();expect(screen.getByLabelText('From (UTC)')).toBeDefined();expect(screen.getByLabelText('To, end-exclusive (UTC)')).toBeDefined();await waitFor(()=>expect(screen.getByText('post-1')).toBeDefined());expect(screen.getByRole('table',{name:'Top privacy-filtered post reads'})).toBeDefined();expect(screen.getByRole('table',{name:'Coarse referrer classes'})).toBeDefined();expect(screen.getByRole('table',{name:'Member access sources'})).toBeDefined();expect(screen.getByRole('table',{name:'Newsletter engagement aggregates'})).toBeDefined();expect(screen.getByText(/No IPs, raw URLs, emails/)).toBeDefined();expect(screen.getByRole('button',{name:'Export deterministic CSV'})).toBeDefined()})
