import { describe,expect,it } from 'bun:test'
import { fixture,release } from './componentCatalogFixture'
describe('FUMA-SITE-008 deterministic demo',()=>{it('authors, versions, previews, inserts and publish-checks one private component',async()=>{const h=fixture();await h.service.execute(h.context,'create',{release:release(),previousCoordinate:null});const preview=await h.service.execute(h.context,'preview',{coordinate:'test.owner/hero@1.0.0',componentId:'hero',variantId:null}) as {component:{displayName:string}};expect(preview.component.displayName).toBe('Hero');await h.service.execute(h.context,'insert',{coordinate:'test.owner/hero@1.0.0',componentId:'hero',usageId:'demo-home-hero',kind:'page-node',resourceId:'home',parentNodeId:'home-root',variantId:null,props:{heading:'Own your site'}});const publish=await h.service.execute(h.context,'publish-check',{}) as {publishable:boolean;exactPins:string[]};expect(publish).toEqual({publishable:true,exactPins:['test.owner/hero@1.0.0']});expect(h.repository.audits.map(a=>a.action)).toEqual(['component.private.created','component.inserted','component.publish.checked'])})})
