import{describe,expect,it}from'bun:test'
import{onboard,scope}from'./domainOperationsTestFixture'
describe('FUMA-062 domain operation concurrency',()=>{it('converges concurrent duplicate inbound commands on one fenced aggregate',async()=>{const f=await onboard();const command=()=>({transferOperationId:'concurrent',domainId:'domain-a',hostname:'www.example.co.ke',authCode:new TextEncoder().encode('SAME-CODE'),authCodeExpiresAt:'2026-07-28T13:00:00.000Z'});const results=await Promise.all([f.service.startInbound(scope,command()),f.service.startInbound(scope,command()),f.service.startInbound(scope,command())]);expect(new Set(results.map((value)=>JSON.stringify(value))).size).toBe(1);expect(await f.repository.registrarTransfer(scope,'concurrent')).toMatchObject({version:1,fence:1,lifecycle:'awaiting-auth-code'})})})
