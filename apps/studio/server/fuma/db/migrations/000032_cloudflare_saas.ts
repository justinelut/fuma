import type { HostedMigration } from '../migrationPolicy'
export const cloudflareSaasMigration:HostedMigration={id:'000032_cloudflare_saas',description:'Add Cloudflare SaaS hostname reconciliation and DNS instructions',sql:`
create table fuma_cloudflare_hostnames (domain_id text primary key references fuma_domains(domain_id) on delete restrict, provider_hostname_id text not null unique, provider_status text not null, ssl_status text not null, reconcile_fence bigint not null check(reconcile_fence>0), last_event_sequence numeric(39,0) not null default 0, included_baseline int not null default 100 check(included_baseline=100), payg_maximum int not null default 50000 check(payg_maximum=50000), additional_unit_usd_cents int not null default 10 check(additional_unit_usd_cents=10), updated_at timestamptz not null);
create table fuma_domain_dns_instructions (domain_id text not null references fuma_domains(domain_id) on delete restrict, record_type text not null check(record_type in ('CNAME','TXT','A')), name text not null, value text not null, purpose text not null check(purpose in ('routing','ownership','tls-validation')), version bigint not null, primary key(domain_id,record_type,name,purpose));
`}
