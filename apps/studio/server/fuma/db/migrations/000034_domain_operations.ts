import type { HostedMigration } from '../migrationPolicy'
export const domainOperationsMigration:HostedMigration={id:'000034_domain_operations',description:'Add customer DNS diagnostics registrar transfers and site transfer outcomes',sql:`
create table fuma_domain_transfers (transfer_id text primary key, domain_id text not null references fuma_domains(domain_id) on delete restrict, direction text not null check(direction in ('inbound','outbound')), state text not null check(state in ('requested','awaiting-unlock','awaiting-auth-code','submitted','completed','failed','rolled-back')), auth_code_ciphertext text null, auth_code_expires_at timestamptz null, provider_reference text null, fence bigint not null check(fence>0), updated_at timestamptz not null, check((auth_code_ciphertext is null)=(auth_code_expires_at is null)));
create table fuma_domain_diagnostics (diagnostic_id text primary key, domain_id text not null references fuma_domains(domain_id) on delete restrict, code text not null, severity text not null check(severity in ('info','warning','error')), observed_json jsonb not null, remediation text not null, checked_at timestamptz not null);
create table fuma_site_transfer_domain_choices (transfer_id text not null, domain_id text not null references fuma_domains(domain_id) on delete restrict, outcome text not null check(outcome in ('retain-with-source','move-with-site','detach-and-manual')), decided_at timestamptz not null, primary key(transfer_id,domain_id));
`}
